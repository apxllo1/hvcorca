- name: Install Rojo
        run: |
          curl -L https://github.com/rojo-rbx/rojo/releases/download/v7.4.1/rojo-7.4.1-linux.zip -o rojo.zip
          unzip rojo.zip
          ls -la
          file rojo || echo "rojo binary not found"
          chmod +x rojo
          sudo mv rojo /usr/local/bin/rojo
          which rojo
          rojo --version
